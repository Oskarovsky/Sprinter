# sprint-feature

Define a new feature to be built in the sprint.

This skill helps in defining a new feature. It combines the process of shaping an idea and creating a Product Requirements Document (PRD).
It should create a `feature-<feature-name>.md` file with the feature definition.
