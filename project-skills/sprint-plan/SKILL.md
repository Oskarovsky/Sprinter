# sprint-plan

Create a plan for implementing the feature.

This skill takes a feature definition and creates a detailed implementation plan. It should create a `features/plan-<feature-name>.md` file.
