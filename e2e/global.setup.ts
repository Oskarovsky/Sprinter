import { test as setup } from "@playwright/test";

setup("do nothing", async () => {
  // This is a placeholder setup file.
  // We can add global setup logic here in the future if needed.
  console.log("Running global setup...");
});
